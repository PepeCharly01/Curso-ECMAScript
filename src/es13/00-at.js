const array = ['one', 'two', 'three', 'four', 'five', 'six'];

console.log(array[array.length -1]);

console.log(array.at(0)); // devuelve el primer elemento.

console.log(array.at(-1)); // devuelve el ultimo elemento.